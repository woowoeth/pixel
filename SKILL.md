---
name: pixelpad
description: 在 24x24 画布上画原生像素图（sprite/图标/头像/游戏素材），逐像素落调色板索引而不是"生成大图再降采样"，按光源方向塑形、多材质色阶、选择性描边，输出透明底 PNG 与逐像素显影 GIF。当用户提到"像素画/像素图/pixel art/sprite/精灵图/24x24/像素头像/游戏素材/像素图标"时使用。Draw native 24x24 pixel art with light-source shading, per-material color ramps and selective outlining.
---

# pixelpad · 24x24 原生像素画

每个像素直接落一个调色板索引——不是生成大图再降采样。你不用手数 576 格，而是写十来行**绘图原语**，引擎执行并渲染。

**画得像不像，靠的不是一次成型，是手艺规则 + 回环。**

---

## 十条手艺规则（不遵守就会画成一团）

### 1. 先过剪影测试
把主体填成纯色，还认得出是什么吗？认不出就是形状的问题，再多的上色也救不回来。
主体占画布 **25%-60%**，四边留 1-3 像素边距。

### 2. 定一个光源方向，按光塑形
**标准是左上角来光。** 别让阴影跟着轮廓走——那是"枕头阴影"（pillow shading），主体会像鼓起来的抱枕，最常见的翻车。

用 `autoshade(base)`：按光源方向自动把迎光边提亮、背光边压暗。

```python
ellipse(11, 10, 10, 6, 3)   # 基色 3 铺形状
autoshade(3)                # 迎光→4，背光→2
```

### 3. 一种材质 = 三个色阶，一张图最多 2-3 种材质

| 索引 | 含义 |
|---|---|
| `0` | 透明背景 |
| `1` | 描边（最深） |
| `2` `3` `4` | 材质 A：暗部 / 基色 / 亮部 |
| `5` `6` `7` | 材质 B：暗部 / 基色 / 亮部 |
| `8` `9` `10` | 材质 C：暗部 / 基色 / 亮部 |

蘑菇 = 伞盖（A 红）+ 菌柄（B 奶油），各 `autoshade` 一次。**超过 3 种材质必然糊。**

### 4. 用 `selout()` 而不是一圈死黑
`outline(1)` 给所有边缘糊纯黑，像贴纸。`selout(1)` 是**选择性描边**：背光侧描深边、迎光侧留空，形体自然得多。
二选一：要么整张硬描边，要么整张 sel-out，**别混着来**。

### 5. 躲开三种伪影
- **双描边**：两块描边挨在一起挤出黑杠 → 挪开一格或改用 `selout()`
- **孤立像素**：单像素飘在外面像噪点 → 删掉或连进形状
- **锯齿乱步**：斜线步长要规整（2-2-2 或 1-2-1-2），别 1-3-1-1-4

引擎每次渲染都会检查这三样并直接告诉你。


### 6. 色相偏移：阴影不只是"变暗"
专业调色板的暗部**色相朝紫蓝偏**、亮部**朝暖黄偏**（模拟日光），不是同一个颜色调明度——那样出来的图会显得廉价。

本引擎的调色板都由 `ramp()` 自动按这个规则生成，你直接用索引即可。想自定义：

```python
build("#1a1420", "#dc3c32", "#e8c88a")   # 描边 + 红材质 + 奶油材质
```

### 7. 抗锯齿有铁律
`aa()` 只对**长度 ≥2 像素的台阶**在拐角补一个半调像素。

**45° 斜线（1×1 步进）和直线绝对不要加** —— 加了只会糊。半调补太多也会糊，宁少勿多。

### 8. 躲开条带（banding）
明暗交界如果沿着轮廓走成一条**等宽长线**，眼睛会看出一条不存在的假边，整个形体会显得平且脏。

`autoshade()` 已经按"迎光细亮边 + 背光较厚暗部、交界贴轮廓弯"来处理；**别自己手工沿边描一圈**。引擎会检测并报警。

### 9. 五官/细节要用**另一种材质**，别用阴影色
把内耳、鼻子、眼睛画成"材质自己的暗色"，它们会混进阴影里变成脸上的暗斑。
细节用另一组材质（如奶油 `6/7`）才跳得出来。这是猫脸从"恶魔"变"猫"的关键一步。

### 10. 小圆用 `dot()`，不要用 `ellipse`
`ellipse` 在半径 ≤2 时数学上会画成菱形/十字。引擎已加 0.35 修正让中大圆变成标准像素圆，
但眼睛这种 2-3 像素的特征，直接用 `dot(x, y, size, c)` 最干净。

---

## 原语

| 原语 | 说明 |
|---|---|
| `rect(x,y,w,h,c)` / `ellipse(cx,cy,rx,ry,c)` / `tri(x0,y0,x1,y1,x2,y2,c)` | 铺形状（主力） |
| `line(x0,y0,x1,y1,c)` | 直线（步长自动摊匀，避免乱步） |
| `dot(x,y,size,c)` | **小特征专用**（眼睛/铆钉/斑点）。小半径别用 `ellipse` —— 会画成十字 |
| `pix(x,y,c)` | 单像素（**最多 8 次**） |
| `autoshade(base, lx=-1, ly=-1)` | 按光源塑形，暗部 `base-1`、亮部 `base+1` |
| `selout(1)` / `outline(1)` | 选择性描边 / 硬描边（**最后一句**） |
| `mirror_x()` / `mirror_y()` | 对称：只画左半 `x=0..11` 再调用 |
| `replace(old,new)` / `shift(dx,dy)` | 换色 / 平移（居中修正） |
| `silhouette()` | 压成纯色剪影 |

坐标 `0..23`，`(0,0)` 左上角。

## 标准写法

```python
ellipse(11, 10, 10, 6, 3)    # 1. 大形状铺主体（材质 A）
rect(8, 15, 7, 8, 6)         #    第二个部件（材质 B）
autoshade(3)                 # 2. 每种材质塑形一次
autoshade(6)
ellipse(6, 8, 2, 1, 7)       # 3. 少量点睛
selout(1)                    # 4. 收尾描边
```

**总语句数 30 行以内。** 三百行 `pix()` 堆出来的一定是噪点。

## 渲染与回环

```bash
python3 pixelpad.py draw sprite.pxl -o out/sprite.png -p classic --gif
python3 pixelpad.py palettes     # classic steel potion forest candy slime mono
```

渲染后打印自检：覆盖率、四边边距、用色、孤立像素、双描边，并直接点名问题。

**然后必须：打开 PNG 真的看一眼。** 问三个问题：

1. 不看提示词认得出吗？→ 认不出就回到剪影重新规划部件
2. 居中吗、够大吗？→ 看 `margin`，用 `shift()` 或放大形状
3. 有体积吗？→ 只有基色和描边就是平的，每种材质各 `autoshade` 一次

改完重渲染再看。**通常两轮，从"一团色"到"能用"。**

## 适合 / 不适合

适合：游戏精灵、图标、头像、道具、食物、动植物、徽章——**单个主体**。
不适合：整幅场景、多角色同框、写实人脸——24x24 装不下。

## 参考件

`examples/` 里六个照上述规则画的样例：
`mushroom`（两材质+斑点）· `cat`（对称镜像）· `sword`（三材质）· `potion`（透明感）· `key`（挖孔）· `tree`（同材质多形状）

## 依赖

Python 3 + Pillow。全本地运行，不调用任何图像生成 API。
