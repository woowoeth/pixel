---
name: pixelpad
description: 在 24x24 画布上画原生像素图（sprite/图标/头像/游戏素材），逐像素落调色板索引而不是"生成大图再降采样"，输出透明底 PNG 与逐像素显影 GIF。当用户提到"像素画/像素图/pixel art/sprite/精灵图/24x24/做个像素头像/游戏素材/像素图标"时使用。Draw native 24x24 pixel art by placing palette indices per pixel (never downscaling a big image); exports transparent PNG plus a pixel-by-pixel reveal GIF.
---

# pixelpad · 24×24 原生像素画

## 这个 skill 在做什么

在 24×24 的画布上，**每个像素直接落一个调色板索引**——不是生成大图再降采样。所以出来的是真正的低分辨率像素画：边缘是硬的，每一格都有人管。

你不用手数 576 个格子。你写十来行**绘图原语**（矩形、椭圆、三角、线、镜像、描边），引擎执行它们并渲染成 PNG。

**最重要的一条：一次画不好是正常的。** 这个 skill 的质量来自「画 → 渲染 → **自己看一眼** → 改 → 再渲染」的回环。跳过"看"这步，出来的东西大概率是个团。

## 工作流程（照这个顺序做）

### 1. 先规划，别急着写代码
把主体拆成 **2–4 个部件**，给每个部件定一块包围盒。24×24 很小，超过 4 个部件必然糊。

例：蘑菇 = 伞盖（上部椭圆，y 0–15）+ 菌柄（下部矩形，y 15–23）+ 斑点（点缀）。

主体应占画布 **25%–60%**，四边留 1–3 像素边距。

### 2. 写 `.pxl` 脚本

每行一句，可用原语：

| 原语 | 说明 |
|---|---|
| `rect(x, y, w, h, c)` | 实心矩形 |
| `ellipse(cx, cy, rx, ry, c)` | 实心椭圆 |
| `tri(x0,y0, x1,y1, x2,y2, c)` | 实心三角（尖角、屋顶、箭头） |
| `line(x0,y0, x1,y1, c)` | 直线 |
| `pix(x, y, c)` | 单像素（**只用于点睛，最多 8 次**） |
| `mirror_x()` / `mirror_y()` | 镜像；对称物体只画左半 `x=0..11` 再调用 |
| `replace(old, new)` | 换色 |
| `shift(dx, dy)` | 整体平移（居中修正用） |
| `outline(1)` | 给所有非空像素加深色描边（**通常是最后一句**） |

坐标 `0..23`，`(0,0)` 在左上角。

**调色板索引固定语义：**

| 索引 | 角色 |
|---|---|
| `0` | 透明背景 |
| `1` | 最深 —— 描边、阴影 |
| `2` | 主色 |
| `3` | 辅色 / 中间调 |
| `4` | 最亮 —— 高光、眼神光、反光 |

### 3. 渲染

```bash
python3 pixelpad.py draw sprite.pxl -o out/sprite.png -p ember --gif
```

- `-p` 调色板：`ink ember forest ocean grape rust mono candy`（`python3 pixelpad.py palettes` 看全部）
- `--gif` 额外导出**逐像素显影动画**（蛇形 4×4 块顺序，像素一格格蹦出来）
- 同时会存一份 `@1x.png` 真 24×24 原图

渲染完引擎会打印**自检报告**：覆盖率、四边边距、用色数、孤立像素数，并直接提示问题（"主体太小""偏了""只用了两种颜色""有噪点"）。

### 4. 看一眼，然后改（**不能跳过**）

**用图片查看工具打开渲染出的 PNG，真的看它。** 然后问自己三个问题：

1. 不看提示词的话，认得出这是什么吗？—— 认不出就是**剪影**的问题，回到第 1 步重新规划部件，不要在细节上打补丁。
2. 它居中吗、够大吗？—— 看自检报告的边距和覆盖率，用 `shift()` 或放大形状修。
3. 有层次吗？—— 只有主色和描边会很平，加 3–6 个 `4` 号高光像素，暗部补 `3`。

改完重新渲染再看。**通常两轮就能从"团状"变成"能用"。**

## 范例

`examples/mushroom.pxl` —— 一次成型的红蘑菇：

```python
ellipse(11, 9, 10, 7, 2)
rect(8, 15, 7, 8, 3)
pix(6, 7, 4)
pix(15, 6, 4)
pix(9, 11, 4)
pix(14, 10, 4)
outline(1)
```

七行，两个大形状 + 四个高光 + 描边。**这就是正确的密度**——不是三百行 `pix()`。

## 常见翻车与对策

| 症状 | 原因 | 怎么改 |
|---|---|---|
| 一团黑/一团色 | 背景被填了非 0 值，或 `outline()` 前主体是空的 | 背景必须留 `0`；确认形状语句真的执行了 |
| 认不出画的是啥 | 部件太多、剪影不清 | 砍到 2–3 个部件，先保证剪影 |
| 看着很平 | 只用了 1–2 个索引 | 加 `4` 高光、`3` 中间调 |
| 满屏噪点 | 大量单像素 `pix()` | 用 `rect`/`ellipse` 铺，`pix` ≤ 8 次 |
| 歪在一边 | 手算坐标偏了 | 看报告 `margin`，用 `shift(dx,dy)` 推回来 |
| 不对称 | 左右分别画 | 只画左半 `x=0..11`，再 `mirror_x()` |

## 适合画什么

游戏精灵、图标、头像、道具（剑/药水/宝箱）、食物、动物头像、表情、徽章。

24×24 画**单个主体**最好。整幅场景、多个角色、写实人脸不适合这个尺寸——那种需求应该换更大的画布或别的工具。

## 依赖

Python 3 + Pillow（`pip install pillow`）。全部本地运行，不调用任何图像生成 API，也不上传你的提示词。
